/** Barrel for KPI-display primitives. Used on every page that shows
 * dashboard-style metrics — admin overview, attorney dashboard, case
 * detail, etc. */

export { KpiCard, type Delta } from "./KpiCard";
export { KpiGrid } from "./KpiGrid";
